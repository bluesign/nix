diff --git a/src/gallium/drivers/zink/zink_screen.c b/src/gallium/drivers/zink/zink_screen.c
index 1111111..2222222 100644
--- a/src/gallium/drivers/zink/zink_screen.c
+++ b/src/gallium/drivers/zink/zink_screen.c
@@ -3370,6 +3370,14 @@ zink_internal_create_screen(const struct pipe_screen_config *config, int64_t dev
       goto fail;
    }

+   /* Force robustness2 features for gfxstream: the host gfxstream backend
+    * auto-enables VK_EXT_robustness2 on device creation, but the guest
+    * gfxstream ICD cannot serialize the feature query structs yet.
+    */
+   screen->info.rb2_feats.nullDescriptor = VK_TRUE;
+   screen->info.rb2_feats.robustBufferAccess2 = VK_TRUE;
+   screen->info.rb2_feats.robustImageAccess2 = VK_TRUE;
+
    if (!screen->info.rb2_feats.nullDescriptor) {
       mesa_loge("Zink requires the nullDescriptor feature of KHR/EXT robustness2.");
       goto fail;
diff --git a/src/gfxstream/guest/vulkan_enc/ResourceTracker.cpp b/src/gfxstream/guest/vulkan_enc/ResourceTracker.cpp
index 3333333..4444444 100644
--- a/src/gfxstream/guest/vulkan_enc/ResourceTracker.cpp
+++ b/src/gfxstream/guest/vulkan_enc/ResourceTracker.cpp
@@ -1848,6 +1848,8 @@ VkResult ResourceTracker::on_vkEnumerateDeviceExtensionProperties(
         "VK_EXT_texture_compression_astc_hdr",
         "VK_EXT_tooling_info",
         "VK_EXT_ycbcr_2plane_444_formats",
+        // Required by Zink for nullDescriptor support
+        "VK_EXT_robustness2",
     };

     VkEncoder* enc = (VkEncoder*)context;
@@ -6920,7 +6922,9 @@ void ResourceTracker::on_vkUpdateDescriptorSetWithTemplate(
                 VkDescriptorBufferInfo* internalBufferInfo =
                     (VkDescriptorBufferInfo*)(((uint8_t*)bufferInfos) + currBufferInfoOffset);
-                VK_FROM_HANDLE(gfxstream_vk_buffer, gfxstream_buffer, internalBufferInfo->buffer);
-                internalBufferInfo->buffer = gfxstream_buffer->internal_object;
+                if (internalBufferInfo->buffer != VK_NULL_HANDLE) {
+                    VK_FROM_HANDLE(gfxstream_vk_buffer, gfxstream_buffer, internalBufferInfo->buffer);
+                    internalBufferInfo->buffer = gfxstream_buffer->internal_object;
+                }
 #endif
                 currBufferInfoOffset += sizeof(VkDescriptorBufferInfo);
             }
